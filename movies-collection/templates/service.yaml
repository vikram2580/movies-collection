apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend
spec:
  ports:
  - port: 5000
    targetPort: 5000
  selector:
    app: backend

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend
spec:
  ports:
  - port: 3006
    targetPort: 3006
  selector:
    app: frontend